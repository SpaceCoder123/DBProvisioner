So basically in a nutshell, this creates a database called test1 (you can modify the appsettings.json file and modify the db name for your convenience)
You cannot modify the existing database so dont create the same database
just hit dotnet run and itll install in your sql server the data and the tables.
